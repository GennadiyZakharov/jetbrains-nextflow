# jetbrains-nextflow-plugin
A plugin for Jetbrains IDE to support Nextflow

## How to build
* use the latest IntelejiIDEa
* Install Platform Plugin SDK
* Run `./gradlew buildPlugin`

## To run a preview
* Run `./gradlew runIde`